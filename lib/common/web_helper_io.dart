// Non-web implementation (does nothing on desktop/mobile)
void openUrlInNewTabImpl(String url) {
  // No-op on non-web platforms
  // On desktop/mobile, this functionality would need url_launcher package
  // or a different implementation
}
